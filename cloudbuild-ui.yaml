steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - ui/Dockerfile
      - -t
      - asia-south1-docker.pkg.dev/$PROJECT_ID/lease-lens-repo/lease-lens-ui:$COMMIT_SHA
      - ui

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - asia-south1-docker.pkg.dev/$PROJECT_ID/lease-lens-repo/lease-lens-ui:$COMMIT_SHA

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - lease-lens-ui
      - --image
      - asia-south1-docker.pkg.dev/$PROJECT_ID/lease-lens-repo/lease-lens-ui:$COMMIT_SHA
      - --region
      - asia-south1
      - --allow-unauthenticated
      - --set-env-vars
      - GOOGLE_CLOUD_PROJECT=$PROJECT_ID

images:
  - asia-south1-docker.pkg.dev/$PROJECT_ID/lease-lens-repo/lease-lens-ui:$COMMIT_SHA

options:
  logging: CLOUD_LOGGING_ONLY
